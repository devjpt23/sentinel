# ── Required ──
NEXT_PUBLIC_API_URL=http://localhost:5252          # Flask API base URL
API_SECRET_KEY=your-api-key-here                   # Flask API key (server-only)

# ── Push Notifications (optional, for browser push) ──
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
